node_modules/
.DS_Store
# Generated by npm run build:algo; CI (pages.yml) rebuilds it at deploy, so the
# deployed search works out of the box. Unlike the sibling tracks' committed
# copies, algo's 200KB+ index stays generated-only — run the build once after
# cloning if you want local search.
algo/data/search-index.json
